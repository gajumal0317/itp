<%= form_with model: rule, url: search_path, method: :get, local: true do |f| %>
<%= form.select :keyword, [ ['投稿が新しい順', 'new'],
                            ['投稿が古い順', 'old'],
                            ['いいねが多い順', 'likes'],
                            ['いいねが少ない順', 'dislikes'],
                          ]%>
<%= f.submit 'Sort', class: 'btn btn-primary btn-sm' %>